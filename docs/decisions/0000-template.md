# NNNN — <short title>

- **Project:** `projects/<project-name>/` (or "repo-wide" if this sets a
  general convention)
- **Date:** YYYY-MM-DD
- **Status:** decided | superseded by NNNN

## The fork in the road

What was ambiguous, and what were the realistic options? Be specific enough
that a future run (with no memory of this conversation) can understand the
choice without re-reading the original issue thread.

## What was decided

State the actual decision plainly, in a way that can be directly applied
(e.g. "Total Revenue = gross line amount, before returns and before tax").

## Why

The user's stated reasoning, if given. If no reasoning was given beyond a
preference, say so rather than inventing a rationale.

## Consequences

What this decision affects downstream — e.g. which measures, which
relationships, whether it changes a total that was already built.
